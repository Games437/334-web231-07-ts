function sum(x: number, y: number): number {
  return x + y;
}

const x = 5;
const y = 7;
const result = sum(x, y);
console.log(`ผลรวมของ ${x} + ${y} = ${result}`);
function greet(name: string): string {
  return `Hello, ${name}!`;
}
console.log(greet("CSMJU"));
console.log(greet("Because of U i Shine"));